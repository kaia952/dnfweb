doctype
html
    head
        meta(charset="utf-8")
        title 地下城详情
        script(src="/public/js/jquery-3.3.1.min.js")
        link(href="/public/css/reset.css" rel="stylesheet")
        link(href="/public/css/common.css" rel="stylesheet")
        link(href="/public/css/page-detail-common.css" rel="stylesheet")
        link(href="/public/css/dungeon.css" rel="stylesheet")
    body
        header
            div.header-returnIndex
                a.header-returnIndex-title(href="/") DNF资料站
                span.header-returnIndex-author made By HD(w-ww)
            if user
                ul.header-list
                    li.header-username 欢迎您，#{user.username}
                    li.header-logout
                        a(href="/logout") 登出
            else
                ul.header-list
                    li.header-login 登录
                    li.header-register 注册
        div.main-container
            each dungeon in result
                div.page-detail-title.clearfix
                    h1 #{dungeon.name}
                    if user
                        if (user.permission >= 10)
                            a.modifyIt(href="/admin/modifyDungeon/#{dungeon.name}") 修改
                    a.returnToList(href="/dungeonlist") 返回地下城列表
                div.content.clearfix
                    div.part.clearfix
                        img(src=dungeon.imgsrc)
                        ul.info-list
                            li
                                span 位置：
                                span #{dungeon.position}
                            li
                                span 首领：
                                span #{dungeon.boss}
                    div.part.part-intro
                        h2 介绍
                        p.intro #{dungeon.intro}
        script(src="/public/js/backToPrev.js")
        script(src="/public/js/headerControl.js")
